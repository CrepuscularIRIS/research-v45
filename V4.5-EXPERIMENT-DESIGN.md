# V4.5 实验阶段设计 — AutoResearch + GSD 链路 + 失败回流

> **驱动 (用户 2026-04-29)**:
> - 实验时长 ~4h (不是 AutoResearch 默认的 5min)
> - 实验失败也有价值 (negative results 也是论文)
> - 失败需要回流到 V4.5 (尝试 cross-products 的下一个 tuple)
> - 用 AutoResearch 做实验骨架 + GSD 链路做 paper-writing

---

## Part 1 — AutoResearch 现状盘点

`~/research/autoresearch/` 已有完整实验骨架:

| 文件 | 作用 |
|---|---|
| `prepare.py` | 数据 + tokenizer + dataloader + eval (read-only) |
| `train.py` | 模型 + optimizer + 训练循环 (唯一可改) |
| `results.tsv` | 实验账本 (untracked, 每 cycle 追加) |
| `program.md` | 5-min budget + val_bpb/peak_vram_mb 指标 |
| `CLAUDE_COMMANDS.md` | `/ar-bootstrap`, `/ar-next`, `/ar-loop`, `/ar-review` |

**优点**: 单 GPU 单 file, 自包含, 已和 GSD/superpowers 集成。
**缺点**: 默认 5min budget, 不适合 4h 实验。

---

## Part 2 — 完整实验链路 (V4.5 + AutoResearch + GSD)

```
用户输入 topic
    ↓
[实验前 — V4.5 已实现]
/v45-decompose topic   →  4 yamls (constitution/assumption/friction/first_principles)
/v45-analyze   topic   →  4 placements + cross-products.yaml (top-K tuples)
/v45-design    topic   →  plan.md (superpowers-ready, 含 N 个 surviving tuples)
    ↓
─────────────────────────────────────────────────────────────
[实验中 — 新设计 V4.5 experiment 链路]
/v45-experiment-init <topic> <tuple-id>
    ↓ (创建 experiments/<topic>/<tuple>/ + AutoResearch-style train.py skeleton)
/v45-experiment-run <topic> <tuple-id> --budget 4h
    ↓ (autonomous train+eval loop, 类似 /ar-loop)
/v45-experiment-verify <topic> <tuple-id>
    ↓ (Opus + Codex 双判 verified/falsified/inconclusive)
─────────────────────────────────────────────────────────────
[3 路 fork 决策]
    ├─→ verified    →  /gsd:gsd-discuss-phase  → paper writing chain
    ├─→ falsified   →  /v45-experiment-pivot   → 取 cross-products 下一个 tuple
    └─→ inconclusive →  /v45-experiment-rerun  → 不同 seed 多跑几次
─────────────────────────────────────────────────────────────
[实验后 — GSD 链路]
/gsd:gsd-discuss-phase    →  clarify findings
/gsd:gsd-plan-phase        →  paper writing PLAN.md
/gsd:gsd-execute-phase     →  draft paper sections
/gsd:gsd-verify-work       →  internal review
/gsd:gsd-ship              →  submit ICLR/ICML/NeurIPS
```

---

## Part 3 — 4 个 v45-experiment-* 命令设计

### `/v45-experiment-init <topic> [<tuple-id>]`

**目的**: 从 plan.md 的某个 tuple 出发,初始化 experiment 工作目录。

**参考 AutoResearch `/ar-bootstrap`**:
- 创建 `~/research/v4/experiments/<topic>/<tuple-id>/` 目录
- 软链 `prepare.py`(read-only 复用 AutoResearch 的)
- copy `train.py` skeleton (从 AutoResearch 取或从 plan.md 生成)
- 创建 `results.tsv` 头
- 创建 `task_plan.md` (planning-with-files 格式)
- 创建 git branch `experiment/<topic>-<tuple-id>`

**Engine**: Opus 4.7 (整合 plan.md 到 train.py skeleton)

**输出**:
```
experiments/<topic>/<tuple-id>/
├── train.py              ← Opus 写的初始模型骨架 (基于 tuple 的 arch + loss)
├── prepare.py → ~/research/autoresearch/prepare.py  (symlink, read-only)
├── results.tsv           ← 实验账本 (header only)
├── task_plan.md          ← planning-with-files 格式的任务拆解
├── findings.md           ← 实验观察记录
├── progress.md           ← 状态追踪
└── .scratch/             ← 临时文件 (gitignored)
```

### `/v45-experiment-run <topic> [<tuple-id>] [--budget 4h]`

**目的**: 跑实验,最多 4h budget,产 results.tsv 行。

**参考 AutoResearch `/ar-loop` + GSD `/gsd:gsd-execute-phase`**:
- 跑 `uv run train.py` (4h timeout)
- 实时 tail log,每 30min 截图给 Opus 看是否继续
- 失败检测: NaN loss / OOM / hang → halt
- 成功: 写一行 results.tsv

**Engine**: Sonnet 4.6 (orchestrator) + Bash (subprocess) — 不是 Opus, 因为这是机械跑实验

**关键: superpowers:test-driven-development 集成**:
- 实验前 Opus 写 `tests/test_<rule>.py` (验证 falsifier 指标计算正确)
- 实验后 Opus 跑 `pytest tests/` 确保 train.py 输出符合 plan.md.falsifier criterion

**输出**:
- `results.tsv` 追加 1 行 (val_bpb, peak_vram_mb, falsifier_metric, ...)
- `run.log` 完整 stdout
- `findings.md` 更新 (异常观察 + hypothesis confirmation/rejection)

### `/v45-experiment-verify <topic> [<tuple-id>]`

**目的**: Opus + Codex 双判实验是 verified / falsified / inconclusive。

**参考 GSD `/gsd:gsd-verify-work` + AutoResearch `/ar-review`**:
- Opus 4.7: 读 results.tsv + plan.md.falsifier → 判 (verified/falsified/inconclusive)
- Codex GPT-5.4: 独立审 — 排除 hyperparameter overfit / cherry-pick / 实现 bug
- 双判一致 → 进 GSD chain
- 双判分歧 → halt, surface to user

**Engine**: Opus + Codex 顺序 (single-pass, convergence)

**输出**:
- `verify.md` (Opus 主判 + Codex 二审 + final verdict)
- 更新 `experiments/<topic>/<tuple-id>/STATUS.json`

### `/v45-experiment-pivot <topic>`

**目的**: 当前 tuple verdict=falsified, 取 cross-products.yaml 下一个 GO tuple 重新跑。

**参考用户洞察**: "实验不行的话,也有实验的道理对吧" — falsified 也是产出, 但要继续推进。

**逻辑**:
1. 读 `experiments/<topic>/<failed-tuple>/verify.md` — 抽 falsified 原因
2. 把 falsified reason 写入 `decompositions/<topic>/frictions.yaml` 的 derailment_priority
   (告诉 V4.5 "这个 friction 在 X 条件下不成立")
3. 从 `placements/<topic>/cross-products.yaml` 找下一个 GO tuple (按 publishability_score 排序)
4. /v45-experiment-init 新 tuple-id (循环)
5. 如果所有 tuple 都跑完了, halt, surface "topic 探索完毕" + 总结 negative results

**Engine**: Sonnet 4.6 (机械 routing)

**输出**:
- `decompositions/<topic>/frictions.yaml` 更新 (添加 falsified 反馈)
- 启动 next tuple 实验

---

## Part 4 — 与 AutoResearch 的差异 + 复用策略

| AutoResearch | V4.5 experiment |
|---|---|
| 单实验单 train.py | 多实验,每 tuple 1 个 train.py (在 experiments/<topic>/<tuple>/) |
| 5-min budget 固定 | 4h budget 可配置 (`--budget`) |
| `/ar-bootstrap`/`/ar-loop`/`/ar-review` | `/v45-experiment-init`/`run`/`verify` |
| 实验失败就 discard | failed verdict 也存 (negative result), 自动 pivot 下个 tuple |
| 单 GPU 单 file | 同样 (复用 AutoResearch 的 prepare.py) |
| 跨 cycle 改 train.py | 跨 tuple 之间共享 prepare.py, 但 train.py 各自独立 |

**复用 AutoResearch 的资源**:
- `prepare.py` 直接 symlink (data prep + tokenizer 不变)
- `kernels` lib 复用 (FlashAttn3 setup)
- `~/.cache/autoresearch/` 数据 cache 共享
- `pyproject.toml` 依赖复用 (uv 环境同一份)

---

## Part 5 — 失败回流的方法论价值

用户洞察 "实验不行也有实验的道理":

```
falsified 不是失败终点, 而是反馈信号:

failed tuple T-F1 (data=MMLU, arch=transformer, loss=MSE)
    ↓ verify.md 写 "MMLU 不适合, 因为 friction 在小样本暴露"
    ↓ 自动更新 frictions.yaml.derailment_priority:
        - friction_id: F1
          score: 7  (downgrade from 8)
          reason: "MMLU empirical kill"
    ↓ /v45-experiment-pivot 自动取 next tuple T-F2
    ↓ T-F2 (data=GSM8K, arch=MoE, loss=MSE) 重新跑
    ↓ 同样的 friction 在 GSM8K 上 verified → paper 出
```

**这是 V4.5 比 AutoResearch 的本质升级**: 失败的 tuple 喂回 frictions.yaml,**让下一次 ideation 看到这个 friction 已经被实验否定过的 placement,避免重复**。

形成 idea-experiment-feedback 闭环。

---

## Part 6 — 决策点 (启动前等用户回答)

1. **AutoResearch 复用方式**: symlink prepare.py (推荐) vs git submodule vs copy?
2. **Budget 配置**: 默认 4h,允许 `--budget 30m | 1h | 4h | 8h` 灵活?
3. **Pivot 自动还是手动**: failed → 自动 pivot vs surface 给用户决定?
4. **Negative results 提交**: failed tuple 是否也走 GSD 写 paper (negative result paper)?
5. **跨 tuple 资源共享**: 多个 tuple 同时跑 (并行 GPU) vs 顺序跑 (单 GPU 排队)?

---

## Part 7 — 实施顺序

| Phase | 命令 | 时间预估 |
|---|---|---|
| 1 | `/v45-experiment-init` | 2h 写 + 测 |
| 2 | `/v45-experiment-run` | 3h 写 + 跑 4h 测试 |
| 3 | `/v45-experiment-verify` | 2h 写 + 测 |
| 4 | `/v45-experiment-pivot` | 1h 写 + 测 |
| 5 | GSD handoff 文档 | 30min |

总: 8.5h 工程 + 至少 1 次 4h 实验跑通验证。

---

## Part 8 — 一行总结

> **V4.5 = 实验前 (Opus 主驱 3 步) + 实验中 (AutoResearch 骨架 + 4h budget) + 实验后 (GSD paper chain)**
> **失败也有价值**: failed tuple 反馈 frictions.yaml → 自动 pivot 下个 tuple → 形成 ideation-experiment 闭环
> **不重写 AutoResearch**: 复用其 prepare.py + kernels + cache, 只 wrap 命令
