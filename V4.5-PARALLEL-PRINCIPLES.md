# V4.5 并行原则 — 在哪些阶段并行、为什么

> **核心问题 (用户 2026-04-29)**: 并行处理得融入实验才有效, 还是 idea 发散就有效?
> **答案**: 都有效, 但**有效的前提不一样**。idea 发散并行需要**物理上下文隔离**, 实验并行天然有效。

---

## Part 1 — V2 失败的并行 (实证)

V2 dispatch 已经有完整并行机制:
- ✅ ONE message → 9 parallel Agent calls
- ✅ Heterogeneous pool: 7 Sonnet + 1 Codex + 1 Gemini
- ✅ Cosine < 0.6 强制 orthogonality 检查
- ❌ **致命缺陷: 9 peers 全读同一 proposals.md + sprint.yaml**

结果 (deep-research 报告 2026-04-29):
- 14 directions × 5 seeds = 45 reframes
- Cross-direction top-3 全是 ICLR-2026 已覆盖
- **即使 9 model 异构、cosine 0.6 阈值都加了**,**因为输入 context 一致就 collapse**

**结论**: 并行 ≠ 多样性。**多样性的根源是 input 不同, 不是 model 不同**。

---

## Part 2 — 并行的 2 类有效场景

### A. Idea-divergence 并行 (实验前)
**前提**: 物理上下文隔离 (输入 yaml/file 不同)
**目的**: 在 hypothesis space 多向探索, 防止早期 collapse
**ROI**: 高 (cheap insurance)

### B. Experiment-execution 并行 (实验中/后)
**前提**: 不同 GPU / 不同 seed / 不同 dataset
**目的**: 加速 + 多 seed 验证
**ROI**: 总是有效 (数据收敛由真实物理决定, 不会同质化)

### C. **NOT 并行** 的场景 (容易踩坑)
- **Novelty audit**: 必须 single 模型见所有 candidates 才能排名
- **Plan writing**: 必须 single 模型整合所有 surviving tuples 写一致 plan
- **Final ranking**: 必须 single 模型见全集才能比较

→ 这些是**convergence 阶段**,**强行并行 = 投票分散 + 一致性丢**。

---

## Part 3 — V4.5 各阶段的并行决策

| 阶段 | 并行? | 隔离方式 | 原因 |
|---|---|---|---|
| **Step 1 Decompose** | YES (双引擎) | Opus prompt ≠ Gemini prompt;两个独立 yaml 输出 | Opus + Gemini 不互看, orthogonal by construction |
| **Step 2 Placement** | YES (4 axes) | data agent 不见 arch agent 输出 | 4 axes 独立, ideation 空间正交 |
| **Step 3 Novelty** | **NO** | — | 必须 single Codex 见 cross-products 全集才能排名 |
| **Step 3 Plan** | **NO** | — | 必须 single Opus 整合 surviving tuples 写一致 plan |
| **Experiment 跑** | YES (per tuple) | 每个 tuple 独立 GPU/seed | 真实物理隔离, 数据自然分化 |

**关键: Step 1 + Step 2 是 idea-divergence 并行 (需要隔离), Step 3 收敛, Experiment 并行天然有效**。

---

## Part 4 — Gemini 的辅助定位 (用户洞察)

V2 已经把 Gemini 用对了:
- **Round A**: Gemini 产 raw paradigm-friction fragments (无 mechanism + 无 citation)
- **Round B**: Opus 反应 Gemini fragments → 加 structure + 写 reframe

V4.5 沿用 + 强化:

| 模型 | 角色 | 输出 | 为什么 |
|---|---|---|---|
| **Gemini 3 Pro** | divergence 助手 | raw fragments + first principles atoms | 训练数据广 + 不需要 structure → 适合飘 |
| **Opus 4.7** | structure 主轴 | constitution + assumption + plan.md | 推理强 + 需要 coherence → 适合稳 |
| **Sonnet 4.6** | placement worker | per-axis ideation yaml | 速度快 + 4 并行不浪费 |
| **Codex GPT-5.4** | convergence judge | novelty audit + ranking | 训练对齐学术 + 必须 single-pass 见全集 |

**Gemini 永远不做 final judge** (V2 已实证: Gemini judge 容易 hallucinate citations)。

---

## Part 5 — V4.5 修正后的命令链路

```
Step 1 Decompose (parallel, isolated)
  ├──→ Gemini: first_principles.yaml      (Gemini 单独跑, 不看 Opus)
  └──→ Opus:   constitution+assumption+friction.yaml  (Opus 单独跑, 不看 Gemini)

Step 2 Placement (parallel, isolated)
  ├──→ Sonnet-1: data-placements.yaml      (只看 frictions.yaml + first_principles.yaml,不看其他 axis)
  ├──→ Sonnet-2: arch-placements.yaml      (同上)
  ├──→ Sonnet-3: loss-placements.yaml      (同上)
  └──→ Sonnet-4: train-placements.yaml     (同上)

Step 3 Convergence (sequential, non-parallel)
  ├──→ Codex: novelty-filter.yaml          (single-pass, 见 cross-products 全集)
  └──→ Opus:  plan.md                       (single-pass, 整合 surviving tuples)

Experiment (parallel by tuple, isolated)
  → tuple-1 → GPU-1, seed-A
  → tuple-2 → GPU-1, seed-B  (后续按 GPU 排队)
  → ...
```

**红线规则**:
1. Step 1 的两个引擎**禁止互看** (写完才合并到 decompositions/<topic>/)
2. Step 2 的 4 个 sonnet **禁止互看** (单 message 同时启动, 4 个独立 cwd 隔离)
3. Step 3 必须 sequential (novelty 完才能 plan)
4. Experiment 跑完才比较 (不在 ideation 阶段比较实验结果)

---

## Part 6 — 直答用户两个问题

### Q1: 并行处理在 idea 假设发散有效吗?

**有效**, 但前提是**物理上下文隔离**。V2 失败因为 9 peers 都读同一 input;V4.5 修正:
- Step 1: Opus + Gemini 各自独立跑, 输出到不同 yaml
- Step 2: 4 sonnet 只看属于自己 axis 的 input slice

→ 并行 + 隔离 = 真发散

### Q2: 并行得融入实验才有效?

**不是必须, 但实验并行 ROI 更高**:
- Idea 阶段并行: 防早期 collapse, cheap insurance (8min 而非 32min)
- 实验阶段并行: 真实物理收敛, 多 seed 验证, ROI 高 (但贵)

**最优组合**: idea 阶段**轻量并行 (3-4 sonnet)** + 实验阶段**重度并行 (per-tuple 独立 GPU)**。

→ 不是二选一, 而是**两阶段都做, 但隔离方式不同**。

---

## Part 7 — 一行总结

> **并行的有效条件 = 输入隔离, 不是 model 异构。**
> V2 把 9 个不同 model 喂同一 context → collapse;
> V4.5 把每个 axis 喂自己的 input slice → orthogonal by construction。
> Gemini 做 divergence 助手 (飘), Opus 做 convergence 主轴 (稳), Sonnet 做 placement worker (快), Codex 做 single-pass judge (准)。
